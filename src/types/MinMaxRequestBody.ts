import CombinationAttributeNames from '~types/CombinationAttributeNames'
import Mod from '~types/Mod'

interface MinMaxRequestBody {
    mod: Mod
    attribute: CombinationAttributeNames
}

export default MinMaxRequestBody
